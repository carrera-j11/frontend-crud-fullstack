# Front-end React (Vite)

## Scripts

```bash
npm install
npm run dev
```

Crea un archivo `.env` basado en `.env.example` para apuntar a la URL de tu API
(local o Railway) mediante la variable `VITE_API_URL`.
